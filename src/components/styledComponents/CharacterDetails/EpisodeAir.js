import styled from "styled-components";

const EpisodeAir = styled.p`
  font-weight: 300;
  font-size: 0.9rem;
  margin-left: 10px;
  font-style: italic;
  color: whitesmoke;
`;
export default EpisodeAir;
